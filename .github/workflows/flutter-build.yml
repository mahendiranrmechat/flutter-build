name: Flutter Windows Build

on:
  push:
    branches: [ main ]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Enable Windows desktop
        run: flutter config --enable-windows-desktop

      - name: Install dependencies
        run: flutter pub get

      - name: Build Windows app
        run: flutter build windows

      - name: List output for debug
        run: Get-ChildItem -Path build/windows/x64/runner/Release/ -Recurse

      - name: Upload Full Windows Build
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: |
            build/windows/x64/runner/Release/demo.exe
            build/windows/x64/runner/Release/flutter_windows.dll
            build/windows/x64/runner/Release/icudtl.dat
            build/windows/x64/runner/Release/data/**
            build/windows/x64/runner/Release/*.dll
          retention-days: 5
